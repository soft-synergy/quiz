'use client'
// Lang-prefixed route — delegates entirely to the shared paywall page component.
// All data comes from the quiz store (language-agnostic).
export { default } from '@/app/quiz/paywall/page'
